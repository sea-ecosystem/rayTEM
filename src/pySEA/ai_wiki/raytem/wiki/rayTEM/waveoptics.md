<!-- AUTO-GENERATED TOC — do not edit; regenerated by gen_ai_index.py -->
| Section | Line |
|---------|------|
| Module-level functions | 22 |
|   `transverse_coordinates(shape, dx, dy)` | 24 |
|   `plane_wave(shape)` | 29 |
|   `gaussian_field(shape, dx, dy, sigma_x, sigma_y)` | 34 |
|   `point_source(shape)` | 39 |
|   `angular_spectrum_propagate(field, dx, dy, wavelength, dz)` | 44 |
|   `focal_phase(field, dx, dy, wavelength, power_x, power_y)` | 49 |
|   `tilt_phase(field, dx, dy, wavelength, tilt_x, tilt_y)` | 54 |
|   `aperture_mask(field, dx, dy, radius)` | 59 |
<!-- END AUTO-GENERATED TOC -->

# waveoptics.py
*`src/pySEA/rayTEM/waveoptics.py`*

Paraxial scalar wave-optics primitives for rayTEM.

---

## Module-level functions

### `transverse_coordinates(shape, dx, dy)` *(line 29)*
Centered real-space coordinate grids for a wavefield.

<!-- AI: add cross-links and non-obvious notes here -->

### `plane_wave(shape)` *(line 53)*
Build a unit-amplitude plane wave (uniform field).

<!-- AI: add cross-links and non-obvious notes here -->

### `gaussian_field(shape, dx, dy, sigma_x, sigma_y)` *(line 69)*
Build a Gaussian amplitude field centered on the grid.

<!-- AI: add cross-links and non-obvious notes here -->

### `point_source(shape)` *(line 91)*
Build a discrete point source (single lit pixel at the grid center).

<!-- AI: add cross-links and non-obvious notes here -->

### `angular_spectrum_propagate(field, dx, dy, wavelength, dz)` *(line 111)*
Propagate a scalar field a distance ``dz`` in free space (paraxial).

<!-- AI: add cross-links and non-obvious notes here -->

### `focal_phase(field, dx, dy, wavelength, power_x, power_y)` *(line 151)*
Apply a (possibly astigmatic) thin focusing phase.

<!-- AI: add cross-links and non-obvious notes here -->

### `tilt_phase(field, dx, dy, wavelength, tilt_x, tilt_y)` *(line 180)*
Apply a linear phase ramp that tilts the wavefront (dipole steering).

<!-- AI: add cross-links and non-obvious notes here -->

### `aperture_mask(field, dx, dy, radius)` *(line 209)*
Apply a hard circular aperture, zeroing the field outside ``radius``.

<!-- AI: add cross-links and non-obvious notes here -->

---
*AI maintenance: read this file before editing the source. Update it after.*
*To regenerate from scratch: delete this file and run `pysea-gen-ai-index`.*
