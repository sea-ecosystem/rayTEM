[project]
name = "pySEA.rayTEM"
version = "0.0.1beta"
description = "Architecture and basic tules for the SEA ecosystem"
readme = "README.md"
requires-python = ">=3.8,<3.18"

dependencies = [
    "ipykernel>=6.30.1",
    "matplotlib>=3.10.6",
    "numpy>=2.3.3",
    "pandas",
    #"sea-eco",
]

[tool.setuptools.packages.find]
include = ["pySEA.rayTEM"]
